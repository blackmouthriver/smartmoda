# Copia a .env y rellena. NUNCA subas .env al repositorio.
# EN-0005 · Entornos y gestión de secretos

# --- Supabase (Fase 1) --------------------------------------------------
SUPABASE_URL=
SUPABASE_ANON_KEY=
# La service_role key NO va aquí ni en el cliente: solo en el servidor.

# --- Azure DevOps (solo para el importador del backlog) -----------------
ADO_ORG=https://dev.azure.com/proyectoOutfit
ADO_PROJECT=Outfit
ADO_PAT=

# --- Firebase (Crashlytics, App Distribution) ---------------------------
FIREBASE_PROJECT_ID=
